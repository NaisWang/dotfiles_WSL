# ===
# === For WSL
# ===
# Acrobat
ext pdf, flag f = wslpath -m "$1" |  xargs Acrobat.exe

# PotPlayer
ext flv|mp4, flag f = wslpath -m "$1" | xargs PotPlayerMini64.exe
# ext flv|mp4, flag f =  '$1'

# WINWORD
ext docx|doc, flag f = wslpath -m "$1"| xargs WINWORD.EXE

# POWERPNT
ext ppt, flag f = wslpath -m "$1" | xargs POWERPNT.EXE

# ImageGlass
ext png|jpg|gif = ImageGlass_sh.sh "$1"
# === #

# ===
# === For Cygwin
# ===
# # Acrobat
# ext pdf, flag f = cygpath -w "$1" | sed 's/\\/\//g' | sed 's/[[:space:]]/\\ /g' | xargs Acrobat

# # PotPlayer
# ext flv|mp4, flag f = cygpath -w "$1" | sed 's/\\/\//g' | sed 's/[[:space:]]/\\ /g' | xargs PotPlayerMini64

# # WINWORD
# ext docx|doc, flag f = cygpath -w "$1" | sed 's/\\/\//g' | sed 's/[[:space:]]/\\ /g' | xargs WINWORD.EXE

# # POWERPNT
# ext ppt, flag f = cygpath -w "$1" | sed 's/\\/\//g' | sed 's/[[:space:]]/\\ /g' | xargs POWERPNT.EXE

# # ImageGlass
# ext png|jpg|gif = ImageGlass_sh.sh "$1"
# === #

# Define the "editor" for text files as first action
mime ^text,  label editor = ${VISUAL:-$EDITOR} -- "$@"
mime ^text,  label pager  = "$PAGER" -- "$@"
!mime ^text, label editor, ext xml|json|csv|tex|py|pl|rb|js|sh|php = ${VISUAL:-$EDITOR} -- "$@"
!mime ^text, label pager,  ext xml|json|csv|tex|py|pl|rb|js|sh|php = "$PAGER" -- "$@"
